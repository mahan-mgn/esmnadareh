# راه‌اندازی دمو

دو مسیر، بسته به اینکه چه چیزی در دسترس توست.

## مسیر ۱ (فعلی) — Cloudflare Tunnel

بدون حساب، بدون کارت، بدون تأیید پیامکی، و برای بازدیدکننده بدون نیاز به VPN.
هر دو میزبان ابری زیر ته مسیرشان کارت اعتباری خواستند، پس عملاً همین راه ماند.

```powershell
powershell -ExecutionPolicy Bypass -File scripts/demo-tunnel.ps1
```

اسکریپت پستگرس لوکال را بالا می‌آورد، در صورت نبود بیلد یکی می‌سازد، اپ را روی
پورت ۳۰۰۰ اجرا می‌کند و یک آدرس عمومی `*.trycloudflare.com` چاپ می‌کند.

نکته‌ها:

- تا وقتی آن پنجره باز است لینک کار می‌کند. بستن پنجره، خوابیدن سیستم یا قطع
  اینترنت یعنی پایان دمو.
- هر اجرا آدرس جدیدی می‌دهد؛ لینک قبلی از کار می‌افتد.
- بیلد باید با `NEXT_PUBLIC_SITE_URL` **خالی** انجام شده باشد تا آدرس مطلق از
  هدرهای `x-forwarded-*` ساخته شود. با مقدار ثابت، باندل به همان آدرس قفل
  می‌شود و تونل بعدی خراب می‌شود.
- **`npm run dev` نباید همزمان روی همان پورت باشد.** اسکریپت این حالت را
  تشخیص می‌دهد و متوقف می‌شود.

### لینک بعد از مدتی از کار می‌افتد

آدرس quick tunnel به نشست فعلی گره خورده است. اگر VPN قطع و وصل شود و آی‌پی
خروجی عوض شود، تونل ثبت‌شده از بین می‌رود و لاگ پر از
`control stream encountered a failure` می‌شود. سرور محلی همچنان سالم است.

اسکریپت با `--protocol http2` اجرا می‌کند تا اتصال روی TCP بماند؛ حالت پیش‌فرض
QUIC روی UDP سوار است و از پشت VPN مرتب می‌افتد. اگر باز هم قطع شد، اسکریپت را
دوباره اجرا کن و آدرس جدید را بفرست.

### صفحه بدون استایل و بدون تعامل باز می‌شود

نشانه‌ی تونل‌شدن سرور توسعه به‌جای بیلد. `next dev` مسیر `/_next/*` را برای هر
Origin بیرون از `allowedDevOrigins` با ۴۰۳ رد می‌کند و هیچ آدرس تونلی در آن
فهرست نیست، پس HTML می‌آید ولی CSS و JS نه.

تشخیصش فریبنده است: ابزارهایی مثل `curl` و `Invoke-WebRequest` هدر `Referer`
نمی‌فرستند و ۲۰۰ می‌گیرند، در حالی که مرورگر ۴۰۳ می‌گیرد. برای بازتولید واقعی:

```powershell
Invoke-WebRequest "$url/_next/static/chunks/<file>.css" -Headers @{ Referer = "$url/" }
```

راه‌حل: سرور dev را ببند، `npm run build` بگیر و دوباره اسکریپت را اجرا کن.

## مسیر ۲ (بایگانی) — Render + Neon

لینک دائمی و ۲۴ ساعته، ولی هر دو سرویس در عمل به کارت اعتباری رسیدند: Vercel
برای شماره‌های ایران تأیید پیامکی ندارد، و Render هم برای Blueprint و هم برای
ساختن دستی Web Service کارت می‌خواهد. پایگاه داده‌ی Neon اما بدون کارت ساخته
شد و مهاجرت‌ها و داده‌ی اولیه رویش اجرا شده است، پس اگر روزی کارت در دسترس بود
فقط قدم ۳ باقی می‌ماند.

از داخل ایران هر دو پنل به VPN نیاز دارند — هم برای راه‌اندازی، هم برای بازدید.

## ۱. دیتابیس روی Neon

1. در [neon.tech](https://neon.tech) با همان حساب گیت‌هاب وارد شو.
2. یک پروژه بساز؛ ناحیه‌ی نزدیک به مخاطب را انتخاب کن (مثلاً Frankfurt).
3. از صفحه‌ی **Connection string** هر **دو** شکل رشته را بردار — کلیدی به نام
   *Connection pooling* آن‌ها را عوض می‌کند:

   | حالت | نمونه | مصرف |
   | --- | --- | --- |
   | با pooling | `...@ep-xxx-pooler.eu-central-1.aws.neon.tech/...` | `DATABASE_URL` |
   | بدون pooling | `...@ep-xxx.eu-central-1.aws.neon.tech/...` | `DIRECT_URL` |

   تفاوتشان فقط `-pooler` در نام میزبان است. `sslmode=require` را در هر دو نگه دار
   و به هر دو **`&connect_timeout=30`** اضافه کن.

   آن `connect_timeout` اختیاری نیست: مهلت پیش‌فرض Prisma پنج ثانیه است و هم
   بیدار شدن compute خوابیده‌ی Neon و هم لینک پرلتانسی (VPN) از آن عبور می‌کنند.
   نتیجه‌اش خطای گمراه‌کننده‌ی `P1001: Can't reach database server` است، در حالی
   که سرور کاملاً در دسترس است.

   پارامتر `channel_binding=require` را که Neon در رشته می‌گذارد می‌توانی نگه
   داری؛ در عمل بی‌اثر بود.

   دو تا لازم است چون pooler در حالت transaction کار می‌کند و قفل و session
   state موردنیاز `prisma migrate` را نگه نمی‌دارد. اپلیکیشن از نسخه‌ی pooled
   استفاده می‌کند و مایگریشن از نسخه‌ی مستقیم — این انتخاب در `prisma.config.ts`
   انجام می‌شود.

## ۲. پر کردن دیتابیس

مایگریشن‌ها موقع هر بیلد روی ورسل خودکار اجرا می‌شوند (اسکریپت `vercel-build`
در `package.json`)، ولی داده‌ی اولیه‌ی کاتالوگ را یک‌بار باید خودت بریزی.
در PowerShell و در ریشه‌ی پروژه:

```powershell
$env:DIRECT_URL = "<رشته بدون pooling>"
$env:DATABASE_URL = "<رشته با pooling>"
npx prisma migrate deploy
npx prisma db seed
```

بعد از تمام شدن، پنجره‌ی ترمینال را ببند تا متغیرها به حالت لوکال برگردند.

### اگر `migrate deploy` وسط کار قطع شد

روی لینک پرلتانسی، خطای `P1017: Server has closed the connection` شایع است.
موتور schema هیچ تلاش مجددی نمی‌کند و پایگاه داده نیمه‌مهاجرت باقی می‌ماند —
جدول‌ها ساخته شده‌اند ولی در `_prisma_migrations` ثبت نشده‌اند، و از آن به بعد
هر `migrate deploy` شکست می‌خورد.

`scripts/neon-apply.mjs` همان SQL را با درایور `pg` و پنج بار تلاش مجدد اجرا
می‌کند و دفترچه‌ی `_prisma_migrations` را دقیقاً مثل خود Prisma می‌نویسد
(همان checksum)، پس اجراهای بعدی `migrate deploy` روی ورسل بی‌اثر رد می‌شوند:

```powershell
$env:DIRECT_URL = "<رشته بدون pooling>"
node scripts/neon-apply.mjs --status   # فقط گزارش وضعیت
node scripts/neon-apply.mjs --reset    # پاک‌سازی و اجرای تمیز همه مهاجرت‌ها
```

`--reset` اسکیمای `public` را کامل drop می‌کند. فقط روی پایگاه داده‌ی تازه یا
دموی دورریختنی استفاده‌اش کن.

برای اطمینان از اینکه Prisma نتیجه را می‌پذیرد:

```powershell
npx prisma migrate status   # باید بگوید: Database schema is up to date!
```

## ۳. دیپلوی روی Render

Vercel برای حساب‌های ایرانی تأیید پیامکی می‌خواهد و پیام
`SMS verification is not enabled for your country` می‌دهد؛ راه دور زدنی بدون
شماره‌ی خارجی ندارد. Render همان کار را بدون شماره و بدون کارت اعتباری انجام
می‌دهد و `render.yaml` در ریشه‌ی مخزن تنظیماتش را از پیش دارد.

1. در [render.com](https://render.com) با حساب گیت‌هاب وارد شو.
2. **New → Web Service** و مخزن `mahan-mgn/esmnadareh` را انتخاب کن.

   > مسیر **Blueprint** (خواندن خودکار `render.yaml`) کارت اعتباری می‌خواهد و
   > با پیام *Add card* متوقف می‌شود. ساختن دستی Web Service این محدودیت را
   > ندارد؛ همان مقادیر را دستی وارد کن:
   >
   > | فیلد | مقدار |
   > | --- | --- |
   > | Language | `Node` |
   > | Branch | `main` |
   > | Region | `Frankfurt (EU Central)` |
   > | Build Command | `npm ci && npx prisma migrate deploy && npm run build` |
   > | Start Command | `npm start` |
   > | Instance Type | **Free** |
   >
   > `render.yaml` را نگه می‌داریم تا اگر بعداً حساب ارتقا یافت، همین تنظیمات
   > بدون بازنویسی قابل استفاده باشند.

3. در بخش **Environment Variables** این پنج مورد را اضافه کن — دو تای اول در
   `render.yaml` بودند و در مسیر دستی باید خودت واردشان کنی:

   | نام | مقدار |
   | --- | --- |
   | `NODE_VERSION` | `22` |
   | `NEXT_PUBLIC_SITE_URL` | `https://esmnadareh.onrender.com` |

   | نام | مقدار |
   | --- | --- |
   | `DATABASE_URL` | رشته‌ی Neon **با** pooling |
   | `DIRECT_URL` | رشته‌ی Neon **بدون** pooling |
   | `AUTH_SECRET` | یک رشته‌ی تصادفی حداقل ۳۲ کاراکتری |

   برای ساختن `AUTH_SECRET`:
   `node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"`

4. **Deploy Web Service** را بزن. اولین بیلد حدود ۵ تا ۱۰ دقیقه طول می‌کشد.
5. اگر نام `esmnadareh` گرفته شده بود و Render آدرس دیگری داد، مقدار
   `NEXT_PUBLIC_SITE_URL` را در **Environment** اصلاح کن و **Manual Deploy →
   Deploy latest commit** بزن. این متغیر موقع بیلد داخل باندل جاسازی می‌شود، پس
   بدون دیپلوی مجدد اثر نمی‌کند و لینک‌های ایمیل و بازگشت از درگاه خراب می‌مانند.

> سرویس رایگان Render بعد از ۱۵ دقیقه بی‌کاری می‌خوابد و درخواست بعدی
> ۳۰ تا ۶۰ ثانیه طول می‌کشد. سهمیه ۷۵۰ ساعت در ماه است.

بقیه‌ی متغیرهای `.env.example` برای دمو لازم نیستند و نبودشان عمداً بی‌خطر است:

- `SMTP_HOST` خالی → ایمیل‌ها به‌جای ارسال در لاگ سرور نوشته می‌شوند.
- `ZARINPAL_MERCHANT_ID` خالی → درگاه شبیه‌سازی‌شده‌ی داخلی فعال می‌شود و
  بازدیدکننده می‌تواند کل مسیر خرید را بدون پرداخت واقعی طی کند.
- `CRON_SECRET` خالی → مسیر `/api/inventory/sweep` بسته می‌ماند. جای نگرانی
  نیست؛ آزادسازی موجودی رزروشده هنگام هر پرداخت و ورود به درگاه هم انجام می‌شود.

## محدودیت‌های شناخته‌شده‌ی این دمو

- **آپلود عکس از پنل ادمین ماندگار نیست.** `src/lib/uploads.ts` روی دیسک
  می‌نویسد و دیسک سرویس رایگان Render با هر دیپلوی از نو ساخته می‌شود، پس
  عکس‌های آپلودشده از بین می‌روند. عکس‌های کاتالوگ چون داخل `public/media`
  کامیت شده‌اند بدون مشکل نمایش داده می‌شوند. برای آپلود ماندگار باید
  ذخیره‌سازی به S3 یا آروان منتقل شود.
- **سرویس رایگان می‌خوابد.** اولین بازدید بعد از بی‌کاری ۳۰ تا ۶۰ ثانیه طول
  می‌کشد. برای دمویی که لینکش را می‌فرستی، بهتر است چند دقیقه قبلش یک‌بار خودت
  بازش کنی تا بیدار باشد.
- دیتابیس رایگان Neon بعد از چند روز بی‌استفادگی به خواب می‌رود؛ اولین بازدید
  بعدی چند ثانیه کند است.
